name: update
description: A package for checking for updates.
homepage: https://github.com/Ampless/update

version: 0.1.0

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  version: ^1.2.0

dev_dependencies:
  pedantic: ^1.9.2
  test: ^1.15.4
  schttp: ^0.2.8
